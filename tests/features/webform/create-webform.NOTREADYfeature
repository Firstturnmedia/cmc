@javascript
@api
Feature: Create webform
  As an admin I create a webform

  Scenario: Create webform test
    Given I am logged in as a user with the "Administrator" role
    # Add webform
    When I go to "admin/structure/webform/add"
    And I fill in "Title" with "Behat Webform Create Test"
    And I wait "2" seconds
    And I press "Save"
    And I wait "2" seconds
    Then I should see "Behat Webform Create Test created."
    # Add elements
    When I go to "admin/structure/webform/manage/behat_webform_create_test"
    And I wait "2" seconds
    And I click "Add element"
    And I wait "2" seconds
    Then I should see "Select an element"
